# Privacy boundary

The public website uses synthetic data only. It does not accept real statements.

The desktop prototype reads a CSV selected by the user and processes it in the local application. It has no account system, analytics, remote database, or financial-data upload path.

This prototype does not yet provide a production encrypted vault. That limitation must remain visible in documentation and release notes until durable encrypted persistence is implemented and reviewed.
